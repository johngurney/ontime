<%= form_with(model: reminder_schedule, local: true) do |form| %>
  <% if reminder_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reminder_schedule.errors.count, "error") %> prohibited this reminder_schedule from being saved:</h2>

      <ul>
      <% reminder_schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
